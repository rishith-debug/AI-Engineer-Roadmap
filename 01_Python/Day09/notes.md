what is a tuple ?
ans :
a tuple is a odered,immutable collection of elements which cant be modified again.

2.what is neagtive indexing ?
ans :
accessing the elements from the end of the tuple.

3.What are the two main built-in methods of a tuple?
ans
the two main built in methods of the tuple are 
count() and inndex()
count(): returns the number of occurrences of a value.
index() : returns the index of the first occurrence of a value.

Q5. What is tuple unpacking?

Answer:
Tuple unpacking is the process of assigning the elements of a tuple to multiple variables in a single statement.

06.diff b/w list and tuple?
ans:
list is a mutable,use[],can be modify,more suitable for changing data.
tuple is a immutable,use(),cannot be modified,more suitable for fixed data.

Q7. Why are tuples immutable?

Answer:
Once a tuple is created, its elements cannot be changed, added, or removed. This makes tuples useful for storing fixed data.

Q8. Can a tuple contain different data types?

Answer:
Yes. A tuple can contain different data types.
