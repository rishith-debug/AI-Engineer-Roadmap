1.what is loops?
loops is a programing control structure that rpeats a specific block of code multiple times until the prdefined condition is met.
each individual of the loop is called iteration.

2.diff btw for and while loops?
forloop(definite iteration)
use a for loop when we know how many time the loop needs to run before even starting the program.
ex : print this line exactly 10 times.

while loop(indefinite iteration)
ue while loop when you dont know ow many times the loop should run in advance.it keeps executing as long as a specific condition remains true.the moment that condition becomes false,it stops.

3.what is range()?
in python range() is a built-in-function used to generate a sequence of numbers.it is most commonly used in for loop to control how many times the loop should execute.

it takes up 3 arguements range(start,stop,step)
start(optional),stop(required),step(optional)


4.what is an infinite loop?
an infinite loop is a loop that repeats infinitely and never stops on its own because its termination condition never met .

5.why do we use loops?
to avoid repetition(dry - dont repeat yourself)
to sacle effortlessly:
to handle dynamic tasks:
Loops save you time, keep your code clean, and allow your programs to handle massive amounts of data instantly..