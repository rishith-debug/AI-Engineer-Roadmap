Q1 . what is the dictionary in python?
Ans = dictionary is a collection of mutable that stores data as key value pairs.

Q2.Can dictionary keys be duplicated?
Ans = No. Keys must be unique. If the same key is added again, its previous value is replaced.

Q3.Can dictionary values be duplicated?

Answer:
Yes. Multiple keys can have the same value.