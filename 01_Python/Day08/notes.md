What is an accumulator variable?
ans
An accumulator is a variable used to store the running result while iterating through a loop.

Q2. Why do we calculate the average after the loop?
ans:
The total is only complete after the loop finishes.
Calculating the average once avoids unnecessary repeated calculations.
It makes the code easier to read and maintain